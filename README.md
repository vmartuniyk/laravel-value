Laravel test
    

## Installation
    Clone the repo and cd into it
    `composer install`
    `npm install`
    `npm run dev`
    Rename or copy `.env.example` file to `.env`
    `php artisan key:generate`
